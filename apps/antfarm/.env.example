# Antfarm Dashboard Configuration

# Product Configuration
# Controls which products are enabled in the dashboard build.
# Comma-separated list of: antfly, termite
# Examples:
#   VITE_PRODUCTS=antfly,termite  - Full antfarm with product switcher (default)
#   VITE_PRODUCTS=antfly          - Antfly-only build
#   VITE_PRODUCTS=termite         - Termite-only build (for standalone embedding)
VITE_PRODUCTS=antfly,termite
